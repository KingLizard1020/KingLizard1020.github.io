#!/bin/sh
# Drop Cursor attribution trailers so squash-merges stay under the repo owner.
msg="$1"
tmp="$(mktemp)"
grep -viE '^[[:space:]]*(Co-authored-by:[[:space:]]*Cursor Agent[[:space:]]*<cursoragent@cursor.com>|Made-with:[[:space:]]*Cursor)[[:space:]]*$' "$msg" > "$tmp"
mv "$tmp" "$msg"
exit 0
